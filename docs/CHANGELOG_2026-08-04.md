# 2026-08-04 修正紀錄

## 核心修正

- 修正 `04-01` 將 `SOURCE_ID`、F14 數值欄與類別欄混合送入 `pivot_longer()` 的錯誤。
- 統一跨檔 join key 型別，並排除 eligibility 技術欄位進入一般類別摘要。
- 兩段式 indicator 同時辨識「沒有」與「無」，保留有效金額守衛與 conflict 記錄。
- 增列 indicator 缺失、有效金額與單碼複選診斷欄。
- 由 2014 原始 `i1` 建立家庭收入組成項 eligibility。
- `05-01`、`05-02` 正式消費 `structural_eligibility.csv`，F8 不再固定為 known-fail。
- 新增修正前／後收支摘要自動對照。
- C3 的 2021 對帳基準固定為 33,921。
- 傳輸檢查封鎖 `.RData`、`.Rhistory` 與意外 RDS。
- 新增全問卷重複代碼掃描；來源同碼多標籤必須以標籤文字辨識，不得以數字猜測。
- 傳輸包排除落後的 `code/main/` 與 `code/archive/`，只提供正式根目錄程式。

## 測試

- 既有摘要規則測試通過。
- 新增兩段式 indicator 單元測試。
- 新增六波合成資料串跑測試，涵蓋 `04-01`、`05-01`、`05-02`、2002 分卷與 2014 `i1`。

## 離線限制

正式六波統計數值仍須在限制性資料環境執行。更新包不含 raw data、個案輸出或正式重跑結果。
